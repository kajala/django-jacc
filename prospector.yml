uses:
    - django

ignore-patterns:
  - (^|/)jacc/migrations(/|$)
  - (^|/)jacc/tests.py(/|$)

pylint:
  disable:
    - consider-using-f-string
    - imported-auth-user
    - useless-suppression
    - django-not-configured
  load-plugins:
    - pylint-django
